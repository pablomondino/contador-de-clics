nota pasos que hago

1 cree la carpeta contador de click dentro de directorio 
proyectos react
-------------------------------------
npx create-react-app contador-de-clics
--------------------------------------
al crearse el proyectose inicializa el repositorio

2 react me inicializa el repositorio
puedo ver si esta inicializado con ls -a 

3 despues creo el repo en github  y tengo que copiar las dos lineas que me descargando
en la segunda poner master en lugar de main

elimino archivos que no sirven



4
clone repositorio
con--->>> git clone y la direccion copiada de github

 deje en 2.55


-podemos incluir imagenes usand import

comilla simple ------->>>>alt+39

en img atributo alt para accesibilidad
en src
en lugar de gacer una cadena de caracteres o una plntilla lit
como usamos en proy anterior
Usamos require
ahora vamos usar import
al usar import queda definida como una variable
y para usarlo pongo llaves( este recurso se asigna al atributo 
crc de la imagen)

-----------------------------
import logo from './imagenes/logo.png'
src={logo}
-----------------------------------
3.02
